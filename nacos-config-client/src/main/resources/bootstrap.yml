spring:
  application:
    name: nacos-config-client
  cloud:
    nacos:
      config:
        enabled: true
        server-addr: 127.0.0.1:8848
        file-extension: yml
  profiles:
    active: dev
server:
  port: 8020
